from django.test import LiveServerTestCase
from selenium import webdriver
from selenium.webdriver.common.keys import Keys

class NewVisitorTest(LiveServerTestCase):
	

# Formulas, Ingredients, Loads, and Bakes will be 	
# assembled in the admin section of app (the interface is excellent and already
# done. The index will be used to display the information with an attractive
# design. 
